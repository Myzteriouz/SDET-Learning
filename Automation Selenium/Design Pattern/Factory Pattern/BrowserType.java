package com.factorypattern.framework;

/**
 * Enum defining the supported browser types in our framework.
 * This prevents hardcoding strings and prevents typos in configuration.
 */
public enum BrowserType {
    CHROME,
    FIREFOX,
    EDGE,
    SAFARI
}
